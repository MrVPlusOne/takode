{
  "name": "the-companion",
  "version": "0.47.0",
  "private": true,
  "packageManager": "bun@1.3.10",
  "description": "Web UI for Claude Code agents",
  "engines": {
    "bun": ">=1.3.10"
  },
  "scripts": {
    "dev": "cd web && bun --no-install run dev",
    "build": "cd web && bun --no-install run build",
    "start": "cd web && bun --no-install run start",
    "prepare": "husky"
  },
  "author": "The Vibe Company",
  "license": "MIT",
  "devDependencies": {
    "husky": "9.1.7"
  }
}
