---
name: takode-memory
description: Search and read curated Takode workstream memory when prior decisions or durable project knowledge may help.
---

# Takode Memory

Use `memory_recall` to find plausible records, then `memory_read` for the exact files needed. Treat results as curated context, not automatically current truth; verify drift-prone facts when practical.

These plugin tools are read-only. Do not claim that memory was updated through them.
